---
title: "ECON 5280: Live R Companion"
format: live-html
webr:
  packages: []
---

This site accompanies the Palatino PDF lecture notes. Each executable cell runs
R directly in the browser through WebR, so the classroom computer does not need
R installed and there is no server session to expire.

Use the navigation menu to open a chapter. Package downloads are declared
chapter by chapter to keep startup time manageable. The first cell on a page may
take a little longer while the WebR engine and that chapter's packages load.

```{webr}
#| autorun: true
#| edit: false
cat("WebR is ready.  The mean of 1:10 is", mean(1:10), "\n")
```

For production causal-forest fits and full bootstrap inference, use the included
GitHub Codespaces configuration. The `grf` trainer requires native threads that
WebR on GitHub Pages cannot supply, so Chapter 6 uses an explicitly labeled
pure-R honest-tree teaching analogue. Chapter 7 demonstrates cross-fitted AIPW
directly. These browser versions remain suitable for a live lecture; the PDF
retains the native-R implementations.
